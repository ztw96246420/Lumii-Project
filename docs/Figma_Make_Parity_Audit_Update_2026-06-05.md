# Figma Make 源码还原审计更新 2026-06-05

## 本轮已修正

- 全局视觉 token：同步 Figma Make `COLORS` 的背景、卡片、边框、主橙色、辅助绿色、正文/次级文字色。
- 字体层级：收敛大量过重的 `800/900` 字重，按源码常用的 `500/600/700` 重新分层。
- 公共按钮/输入框：按钮高度、圆角、字重对齐源码；Web 预览移除默认黄色 focus 外框。
- 底部导航：从黑色选中胶囊改为源码风格的白色玻璃底 + 浅橙选中态；发现、地图、消息、我的均已抽检。
- 登录倒计时：展示最大值夹到 60s，避免请求返回瞬间出现 61/62s。
- 权限页：移除额外的顶部标题，恢复更接近 Figma Make Screen10 的空标题 TopBar 结构。
- 电子宠物首页：按 Screen26 源码恢复四个 quick tile：
  - 今日体重
  - 疫苗提醒
  - 健康备忘
  - 附近伙伴
- 电子宠物首页：补回源码里的「记录今天的小事」横条，并调整首屏布局，避免被 TabBar 遮挡。
- 消息页/我的页：复核后均只有一个页面标题，不再出现重复标题。
- 地图页：复核搜索条、筛选 chip、右侧控件、底部地点 sheet、TabBar 层级，当前比上一版更接近源码结构。

## 浏览器抽检结果

- 登录页：手机号输入未复现逐字失焦；协议点击不跳页；Web focus 黄框已处理。
- 验证码页：可进入；测试验证码为 `962464`；倒计时逻辑继续保留。
- 权限页：主体结构正常，顶部标题已按源码修正。
- 空宠物页：TabBar 样式已回到源码方向，底部无遮挡。
- 建档 -> 上传 -> 识别 -> 生成 -> 保存 -> 首页：主链路跑通。
- 添加宠物后的首页：首屏不再遮挡故事横条，quick tile 与源码内容结构一致。
- 发现页：无重复标题，卡片与筛选结构可用。
- 地图页：全屏地图结构可用，底部 sheet 与 TabBar 层级可接受。
- 消息页：无重复标题。
- 我的页：无重复标题，用户卡、当前宠物卡、菜单组结构可用。

## 仍不能宣称 1:1 的部分

- 目前仍未做逐像素坐标表对照，只是源码结构 + 浏览器截图抽检。
- RN Web 和 Figma Make 的字体渲染、图片裁切、阴影会有天然差异，需要继续逐屏截图比对。
- 权限拒绝、验证码错误/过期、上传失败、生成失败、保存失败等异常态还需要单独截图验收。
- 多宠管理、编辑宠物、招呼请求、约遛邀请、地点新增/点评/审核状态、账号注销等 P1/P2 页面仍不是完整 1:1。
- 地图页真实高德 SDK 接入后，当前 mock 地图还需要重新适配视觉层。

## 下一轮建议顺序

1. 继续做登录/验证码全状态截图矩阵。
2. 做权限拒绝/blocked 三端状态截图矩阵。
3. 做上传失败、未检测到宠物、AI 生成失败、保存失败四个异常页。
4. 对 Screen26 首页做一轮更细的坐标和字号表，确认宠物形象大小是否接受当前 RN 适配值。
5. 再推进消息详情、招呼请求、约遛邀请、地点新增/点评、多宠管理等缺口页面。
