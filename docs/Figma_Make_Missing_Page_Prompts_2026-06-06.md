# Lumii Figma Make 缺失页面与状态提示词

维护日期：2026-06-12

## 使用规则

- 这份文档只记录“还没有设计源、不能由前端自行发挥”的页面、弹层、复杂状态和关键交互。
- 已经有 React Native 路由或基础实现的页面，不再列为“缺失页面”；如果只是缺某个细分状态，会明确写成“缺失状态/弹层/组件”。
- 新页面、新 Bottom Sheet、新复杂弹窗、新卡片样式、新富消息样式，必须先由用户在 Figma Make 补齐设计，再由 Codex 转译实现。
- 当前开发优先级：先做已有页面内的逻辑、状态联动、接口契约、缓存、Bug 修复；尽量暂缓新增页面和外部模型/API。
- 视觉源优先级：Figma Make `Lumii Project Opus4.7` > 源码包 > 截图比对 > Codex 临时 scaffold。

## 当前已实现/已有基础页面

以下内容不要再误认为“缺失页面”。后续如果要增强，只按“缺失状态/弹层/组件”记录。

- 登录页、输入验证码页、权限页。
- 未添加宠物页、宠物信息页、编辑宠物信息页。
- 上传照片页、识别详情页、未检测到宠物页、生成中页、形象确认页。
- 电子宠物首页、AI 宠物对话页。
- 健康管理页、健康备忘列表页、体重页、疫苗计划页。
- 发现页、招呼请求页、约遛页。
- 地图页、地点详情页、新增地点基础页。
- 消息列表页、通知中心页、聊天详情基础页。
- 我的页、宠物档案页、设置与隐私页、账号安全页、安全中心页、今日小事页。

## 当前确实缺失的 Figma Make 设计

### 0. ~~健康日历总览页~~

状态更新（2026-06-15）：新版 Figma Make 源码包 `Lumii Project - Opus4.7 (2).zip` 已补齐 Screen75-80；React Native 已新增 `healthCalendar` route，接入 `GET /health/calendar`，覆盖默认月视图、选中日期有事件、选中日期空状态、逾期/临近提醒、加载/下拉刷新、读取失败。该项不再作为缺失页面提示词保留，后续只做截图级 1:1 视觉复核。

状态更新（2026-06-18）：产品入口收敛为“健康日历”承载体重、疫苗/驱虫和健康备忘记录；宠物首页、健康页近期记录、宠物详情健康区均进入健康日历。健康备忘列表页不再作为主流程入口，仅保留兼容。健康日历顶部宠物小卡删除健康分展示，避免和首页/健康首页重复。

类型：新页面。当前健康管理页、健康备忘页、体重页、疫苗计划页已有基础实现；这里不重复要求这些页面，只记录尚未原生化的“健康日历总览”独立页面。

什么时候需要：用户希望按日期查看体重、疫苗/驱虫、健康备忘等所有健康事件时。

接口状态：`GET /health/calendar` 已有 MVP 测试后端和 mock API 支持，会按当前宠物聚合体重、疫苗和健康备忘；这里只缺 Figma Make 的独立日历页设计。按当前开发优先级，暂缓开发。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“健康日历总览页”，尺寸为 iPhone 竖屏。注意：健康管理页、体重页、疫苗计划页、健康备忘页已经存在，不要重新设计这些基础页面；本次只设计按日期聚合健康事件的日历总览。

视觉风格严格延续 Lumii Project Opus4.7：浅暖背景、白色柔和卡片、真实卡通化宠物头像、青绿色健康提示、橙棕色主按钮、圆润但克制，所有文案必须为简体中文。

页面需要包含：
1. 顶部返回栏，标题“健康日历”。
2. 当前宠物小卡：宠物头像、昵称、最近状态说明；不要展示健康分。
3. 月份切换区域：当前月份、上一月/下一月图标按钮。
4. 日历网格：每一天用小圆点或轻量标签显示当天是否有体重、疫苗、备忘事件。
5. 选中日期状态：高亮选中日期，并在下方展示该日事件列表。
6. 事件列表卡片：体重记录、疫苗/驱虫计划、健康备忘三种类型要有不同但克制的图标或色点。
7. 空状态：选中日期无事件时显示“这一天还没有健康记录”。
8. 即将到期提醒：疫苗/驱虫临近或逾期时，用温和提示，不制造医疗焦虑。
9. loading、读取失败、下拉刷新、重试按钮状态。

请同时生成默认月视图、选中日期有事件、选中日期为空、疫苗即将到期/逾期、加载中、读取失败六种状态。
```

### 1. ~~多宠管理与切换~~

状态更新（2026-06-12）：新版 Figma Make 源码包 `Lumii Project - Opus4.7 (2).zip` 已补齐 Screen81-85；React Native 已新增 `multiPet` 路由，支持宠物列表、当前宠物展示、切换 loading、切换成功 toast、添加入口、删除二次确认，并接 `GET /pets`、`POST /pets/{id}/set-default`、`DELETE /pets/{id}`。

类型：新页面/新复杂列表。

什么时候需要：支持同一账号添加多只猫狗、切换当前宠物、管理宠物列表时。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“多宠管理与切换”页面，尺寸为 iPhone 竖屏。整体风格严格延续 Lumii Project Opus4.7：温暖浅色背景、柔和卡片、真实卡通化宠物头像、橙棕色主按钮、圆润但克制的控件、所有文案为简体中文。

页面需要包含：
1. 顶部返回栏，标题“我的宠物”。
2. 当前宠物概览卡：宠物头像、昵称、品种、年龄/生日、体重、当前使用标记。
3. 宠物列表卡片：每只宠物展示头像、昵称、猫/狗类型、品种、最近健康状态；当前宠物显示“当前灵伴”标签。
4. 每张卡支持“切换为当前宠物”按钮；当前宠物按钮置灰或显示已选中。
5. 页面底部或列表末尾有“添加新的宠物”入口。
6. 空状态：还没有宠物时展示温暖插画区域、文案“先添加一位灵伴吧”、按钮“添加宠物”。
7. 交互状态：切换中 loading、切换成功 toast、切换失败 toast、删除宠物入口的危险操作二次确认占位。

请同时生成默认列表态、空状态、切换 loading 态、切换成功 toast、删除确认弹窗。
```

### 1.1 ~~个人资料编辑页~~

状态更新（2026-06-12）：新版 Figma Make 源码包已补齐 Screen86-91；React Native 已新增 `ownerEdit` 路由，支持头像选择占位、昵称/简介校验、手机号只读、保存 loading、成功/失败 toast，并扩展 `PATCH /me` 支持 `ownerName`、`ownerBio`、`ownerAvatarUrl`。

类型：已有我的页的新增编辑页面/弹层。

什么时候需要：用户点击“我的页”头像区域的编辑入口，修改主人昵称、头像或个人简介时。

接口状态：`PATCH /me` 已支持更新 `ownerName`、`ownerBio`、`ownerAvatarUrl`；头像当前保存本地/URL 占位，正式对象存储上传仍是后续生产增强。~~当前 App 不自行新建设计，点击编辑入口只提示“个人资料编辑需补充 Figma 设计后开放”。~~

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“个人资料编辑页”，尺寸为 iPhone 竖屏。整体风格严格延续 Lumii Project Opus4.7：温暖浅色背景、柔和白色卡片、圆润但克制的控件、橙棕色主按钮，所有文案必须为简体中文。

页面需要包含：
1. 顶部返回栏，标题“编辑个人资料”。
2. 主人头像区域：圆形头像、相机小按钮、头像上传/更换状态占位。
3. 昵称输入框：标题“主人昵称”，默认填入当前昵称，支持清空错误提示和保存中状态。
4. 手机号只读项：显示脱敏手机号，说明“手机号暂不支持修改”。
5. 可选简介输入框：placeholder“写一句介绍你和毛孩子的话”。
6. 底部主按钮“保存资料”，保存中 loading、保存成功 toast、保存失败 toast。
7. 字段校验：昵称为空、昵称过长、网络失败。

请同时生成默认态、头像更换中、昵称错误、保存中、保存成功 toast、保存失败 toast。
```

### 2. ~~健康备忘编辑与删除状态~~

状态更新（2026-06-12）：新版 Figma Make 源码包已补齐 Screen92-96；React Native 已新增 `memoEdit` 路由，健康备忘列表可点击进入编辑，支持标题/内容校验、保存 loading、保存成功 toast、删除二次确认、删除后返回列表，并接 `PATCH /health/memos/{memoId}`、`DELETE /health/memos/{memoId}`。

状态更新（2026-06-18）：健康备忘列表页已退出主流程入口；新增、编辑、删除备忘后的主返回位置改为健康日历。编辑/删除状态仍保留，因为健康日历事件可以进入备忘编辑。

类型：已有健康备忘列表页的新增编辑页/弹层/删除确认。

什么时候需要：用户编辑、保存、删除健康备忘记录时。

接口状态：`PATCH /health/memos/{memoId}` 和 `DELETE /health/memos/{memoId}` 已有 MVP 测试后端支持；这里只缺 Figma Make 的编辑态、删除确认和相关 toast 设计。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“健康备忘编辑与删除状态”，尺寸为 iPhone 竖屏。延续 Lumii 当前健康模块视觉：浅暖背景、白色圆角卡片、青绿色健康提示、橙棕色主按钮、简体中文。

页面需要包含：
1. 顶部返回栏，标题“编辑备忘”。
2. 备忘标题输入框，示例“洗澡记录”。
3. 多行备忘内容输入框，示例“今天洗澡后耳朵干净，皮肤没有明显泛红。”
4. 日期/分类信息行，例如“今天 · 日常护理”。
5. 保存按钮，文案“保存修改”。
6. 删除入口，危险色文字或按钮“删除备忘”。
7. 删除二次确认弹窗：标题“删除这条备忘？”，说明“删除后不可恢复”，按钮“取消”“确认删除”。
8. 保存成功 toast、删除成功 toast、内容为空错误提示。

请同时生成默认编辑态、保存中、保存成功 toast、删除确认弹窗、删除后空状态。
```

### 3. ~~体重记录编辑/删除与趋势异常状态~~

状态更新（2026-06-12）：新版 Figma Make 源码包已补齐 Screen97-101；React Native 体重页已补趋势卡、SVG 折线、温和异常提示、历史记录点击编辑、底部编辑 Sheet、保存 loading、删除二次确认和空状态，并接 `PATCH /health/weights/{weightId}`、`DELETE /health/weights/{weightId}`。

类型：已有体重页的缺失状态/弹层。

什么时候需要：用户编辑历史体重、删除错误记录、看到体重异常提醒时。

接口状态：`GET /health/weights/trend`、`PATCH /health/weights/{weightId}`、`DELETE /health/weights/{weightId}` 已有 MVP 测试后端和 mock API 支持；这里只缺趋势详情、编辑/删除弹层、删除二次确认和异常态视觉设计。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“体重记录编辑删除与趋势异常状态”，尺寸为 iPhone 竖屏。延续 Lumii 健康模块风格，整体轻量、清晰、适合日常记录。

需要包含：
1. 体重趋势卡：折线图区域、当前体重、最近变化、正常范围提示。
2. 历史记录列表：日期、体重 kg、备注、编辑入口。
3. 编辑体重弹层：数字输入、日期选择、备注输入、保存按钮。
4. 删除体重记录二次确认弹窗：说明删除后趋势会重新计算。
5. 异常提醒状态：例如“近 30 天体重变化较快”，给出温和提示，不制造焦虑。
6. 空状态：暂无体重记录，按钮“记录第一次体重”。
7. loading、保存成功 toast、保存失败 toast、删除成功 toast。

请生成趋势正常态、趋势异常态、编辑弹层、删除确认弹窗、空状态。
```

### 4. 聊天详情富消息与长按菜单

类型：已有聊天详情基础页的缺失组件/状态。

什么时候需要：宠物主人之间发送图片、地点、约遛邀请，或消息失败重试时。

当前实现状态：
- 普通文本消息、发送失败重试/删除、会话刷新、约遛邀请卡片识别已经接入。
- 会话里的约遛邀请卡片已支持“接受”和“稍后确认”，会发送真实文本回复。
- 会话底部“约遛”附件已接到现有约遛邀请页。
- 会话底部“地点”附件已支持发送当前选中的宠物友好地点文字消息；完整地点卡片样式仍在本节保留。
- 会话底部“宠物卡”已能发送当前宠物资料文本。
- 仍缺完整富消息视觉与数据结构：图片消息、地点卡片消息、真正的宠物卡片消息、约遛“已接受/改时间/取消”细分状态、长按菜单、对方输入中。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“聊天详情富消息与长按菜单状态”，尺寸为 iPhone 竖屏。注意这是宠物主人之间的聊天，不是 AI 灵伴对话。视觉风格延续 Lumii：浅暖背景、白色内容区、柔和气泡、隐私安全提示轻量展示，所有文案为简体中文。

页面需要包含：
1. 顶部聊天对象栏：返回按钮、对方宠物头像、主人/宠物名、副标题“模糊距离 · 已互相打招呼”、举报入口图标。
2. 安全提示条：“聊天中请勿透露精确住址，线下见面建议选择公开宠物友好地点。”
3. 普通文字消息气泡：对方消息、自己消息。
4. 图片消息气泡。
5. 地点卡片消息：地点名、地址、模糊距离、小地图缩略感、按钮“查看地点”。
6. 约遛邀请卡片：地点、时间、状态“待确认/已接受/已取消”，按钮“接受”“改时间”。
7. 发送失败状态：消息旁显示失败标识和“重试”。
8. 对方输入中状态。
9. 底部输入栏：文本输入、图片按钮、地点按钮、发送按钮。
10. 长按消息菜单：复制、删除、举报。

请同时生成默认聊天、发送中、发送失败、对方输入中、长按菜单、约遛卡片待确认/已接受状态。
```

### 5. 发现页搜索筛选 Bottom Sheet 与异常状态

类型：已有发现页的缺失弹层/状态。

什么时候需要：用户筛选猫狗、距离、互动意愿，或定位/附近列表异常时。

当前状态：RN 已先在现有发现页实现搜索展开和本地关键词过滤；这里仍主要缺复杂筛选 Bottom Sheet、距离范围、加载 skeleton 和更完整异常态设计。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“附近发现搜索筛选与异常状态”，尺寸为 iPhone 竖屏。页面用于搜索附近宠物主人和筛选猫狗主人。保持 Lumii 温暖、高级、轻社交、隐私友好的风格。

需要包含：
1. 发现页顶部标题“发现”，右侧搜索和筛选图标。
2. 搜索展开状态：搜索框 placeholder“搜索宠物名、品种、兴趣标签”。
3. 筛选 Bottom Sheet：宠物类型（全部/狗狗/猫咪）、距离范围（1km/3km/5km）、互动意愿（想交朋友/可约遛/只线上聊天），按钮“重置”“查看结果”。
4. 筛选结果列表卡片，延续当前附近宠物卡片风格。
5. 定位未授权状态：说明开启定位才能发现附近伙伴，按钮“去开启”。
6. 附近为空状态：文案“附近暂时没有新的伙伴”，按钮“扩大范围”。
7. 筛选无结果状态：文案“没有符合条件的伙伴”，按钮“清空筛选”。
8. 卡片加载 skeleton 状态。

请同时生成搜索展开、筛选面板、定位未授权、附近为空、筛选无结果、加载中状态。
```

### 6. 打招呼与约遛发送状态

类型：已有发现/约遛流程的缺失 Bottom Sheet/状态。

什么时候需要：用户从发现页对附近主人打招呼、发起约遛邀请时。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“打招呼与约遛发送状态”，尺寸为 iPhone 竖屏。整体延续 Lumii 社交模块风格：轻松、友好、不过度打扰，强调模糊距离与安全感。

需要包含：
1. 主人和宠物迷你资料 Bottom Sheet：头像、宠物名、品种、模糊距离、共同兴趣标签。
2. 打招呼快捷语选择：例如“你好呀，想认识一下你家毛孩子”“附近一起散步吗？”。
3. 自定义一句话输入框。
4. 发送按钮“打个招呼”。
5. 发送中 loading、发送成功状态、发送失败可重试状态。
6. 约遛邀请表单：地点、日期、时间、备注，按钮“发送邀请”。
7. 安全提示：首次见面建议选择公开宠物友好地点。

请生成打招呼默认态、发送中、发送成功、发送失败、约遛表单态。
```

### 7. 地点审核、分享、导航确认与异常状态

类型：已有地图/地点/新增地点基础页的缺失状态/弹层。

什么时候需要：用户新增地点、点评地点、分享地点、打开高德导航时。基础地图页、地点详情页、新增地点页已经存在；基础重复地点服务端拦截也已完成，这里仅补复杂状态、弹层和异常态。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“地点审核分享与导航确认状态”，尺寸为 iPhone 竖屏。属于宠物友好地点模块，延续 Lumii 地图页温暖白色底部 sheet、橙棕色主行动、轻量地图元素的视觉。

需要包含：
1. 新增地点提交后的审核中状态：标题“已提交审核”，说明“通过后会展示给附近用户”，展示审核单号占位。
2. 点评提交后的审核中状态：标题“点评已提交”，说明“审核通过后会展示在地点详情中”。
3. 重复地点提示：标题“可能已存在相同地点”，列出疑似地点卡片，按钮“查看已有地点”“仍然提交”。
4. 提交失败状态：错误说明，按钮“重新提交”。
5. 收藏/取消收藏 toast 状态。
6. 分享地点 Bottom Sheet：分享到聊天、复制链接、系统分享。
7. 外部导航确认弹窗：高德地图优先，按钮“取消”“打开高德地图”。

请生成审核中、重复地点、提交失败、分享面板、导航确认弹窗；不要重新设计基础地图页、地点详情页或新增地点基础页。
```

### 8. ~~AI 生成多候选与“不像我家宠物”反馈~~（RN 已接入，后续仅视觉复核）

类型：已有形象确认页的缺失状态/组件。

什么时候需要：真实 AI 生成链路稳定后，支持多张候选图、重新生成和相似度反馈。

当前接口/页面状态：基础生成、轮询、保存、重新生成、任务反馈接口已接入 MVP 测试后端和 mock API；React Native 已按新版 Figma Make 源码补齐 Screen21-25 的三候选 UI、反馈原因 Bottom Sheet、重新生成确认弹窗、保存成功/失败 Toast。这里不再作为缺失页面提示词保留，后续只做截图级视觉复核和真实多候选接口替换。

已处理记录（2026-06-15）：`renderAiResult` 新增候选切换和“不像我家宠物”反馈入口；`renderAvatarFeedbackSheet` 接 `sendGenerationFeedback`；`renderAvatarRegenerateConfirm` 替代普通确认弹窗；`Toast` 增加副文案以覆盖 Screen24/25。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“AI 宠物形象多候选与反馈状态”，尺寸为 iPhone 竖屏。风格必须保持真实卡通化：保留真实宠物五官比例、毛色、耳朵和表情特征，不要过度拟人，不要衣服。页面视觉延续 Lumii 生成结果页。

需要包含：
1. 顶部标题“选择你的灵伴形象”。
2. 原始照片小圆图和生成结果对比区域。
3. 2-4 张候选卡片网格，支持选中状态。
4. 主按钮“保存这个形象”。
5. 次级入口“重新生成”。
6. 反馈入口“不像我家宠物”，展开后可选择原因：毛色不像、脸型不像、表情不像、不是同一只、画风不喜欢。
7. 重新生成中 loading、重新生成失败、保存失败、保存成功 toast。

请生成候选默认态、选中态、反馈展开态、重新生成中、失败态。
```

### 9. 宠物照片裁剪/主体选择与上传拦截

类型：新增页面/弹层。当前暂不接视觉识别模型，但先预留设计。

什么时候需要：照片里有人+宠物、多只宠物、宠物+其他动物、无宠物、主体不清晰时。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“宠物照片裁剪与主体选择”流程，尺寸为 iPhone 竖屏。用于上传真实宠物照片后，在生成形象前确认主体。风格延续 Lumii 上传识别页，简洁、温暖、降低用户挫败感。

需要包含：
1. 照片预览区域，支持裁剪框视觉。
2. 提示文案“请选择要生成灵伴形象的宠物”。
3. 多宠物检测状态：在照片上用柔和描边标出候选宠物 A/B，底部显示候选缩略图。
4. 人和宠物同框状态：温和提示“我们只会使用宠物部分生成形象”，支持裁剪到宠物区域。
5. 无宠物状态：标题“没有检测到清晰宠物”，说明“请上传一张宠物正脸或半身照片”，按钮“重新选择”。
6. 主体不清晰状态：提示补拍建议。
7. 确认按钮“使用这只宠物”。
8. 返回重新上传入口。

请生成单宠清晰、多宠选择、人宠同框裁剪、无宠物、主体不清晰五种状态。
```

### 10. 安全中心细分：举报、拉黑、黑名单、注销账号

类型：已有安全中心/账号安全页的缺失细分页面和危险操作弹窗。

什么时候需要：上线社交功能前补齐安全闭环。

Figma Make 提示词：

```text
请为 Lumii/灵伴移动端 App 生成“安全中心举报拉黑与账号注销流程”，尺寸为 iPhone 竖屏。视觉风格克制、安全、清晰，不要制造压迫感。所有危险操作必须有二次确认。

需要包含：
1. 举报表单：举报对象信息、举报原因选项、补充说明、提交按钮。
2. 拉黑确认弹窗：说明拉黑后对方无法查看资料、宠物和位置，也无法发消息。
3. 黑名单管理页：用户列表、解除拉黑按钮、空状态。
4. 解除拉黑确认弹窗。
5. 账号注销流程：风险说明、数据影响清单、冷静期说明、验证码确认入口、最终确认按钮。
6. 提交成功/失败 toast。

请生成举报表单、拉黑确认、黑名单列表、黑名单空状态、解除确认、注销账号说明、最终确认弹窗。
```

## 暂不需要 Figma，可继续开发的内容

这些属于已有页面内的逻辑或后端契约，不需要你先补新设计：

- ~~二次登录免验证码/会话缓存的细化和边界测试。~~ 已补 `POST /auth/token/refresh` 与启动刷新账号快照；401 才清本地 session 回登录。App 已补登录恢复 token 当前性校验和刷新后 session 快照持久化，连续登录、切号或退出后的旧宠物/权限回包不会误改当前首屏路由。
- ~~MVP 登录态 token 签名与刷新。~~ 已改为服务端签发 `lumii-v1.<payload>.<signature>` HMAC token，并兼容旧 `lumii-local-手机号`；这是后端/API 能力，不需要新增 Figma 页面。
- ~~退出登录当前 token 撤销。~~ 已在 `POST /auth/logout` 撤销当前签名 token，退出后旧 token 访问会 401；复用现有退出账号入口，不需要新增页面。
- ~~短信验证码每日频控。~~ 已在现有登录/验证码状态里复用 toast 和倒计时逻辑，不需要新增页面；MVP 测试后端已支持单手机号/设备/IP 每日上限，生产短信服务商回执、随机码和更完整风控另行确认。
- ~~验证码成功后消费票据。~~ 已在测试后端和 mock API 完成，避免同一验证码票据重复使用或过期残留误挡；复用现有登录/验证码页面，不需要新增 Figma。
- ~~验证码输错次数限制。~~ 已在测试后端和 mock API 完成，同一张票据默认最多输错 5 次，达到上限后复用现有验证码错误 toast 和重新获取倒计时，不需要新增 Figma。
- ~~登录验证码前端发码/验码状态保护。~~ 已在现有登录页和验证码页内完成 ref 级互斥、倒计时前端拦截、手机号一致性校验和验证码票据一致性校验；不需要新增 Figma 页面。
- ~~统一错误码结构。~~ 已在测试后端、HTTP API 门面和 mock API 保留 `error.code`，现有页面继续展示 `error.message`，不需要新增页面。后续如果要做“错误详情/帮助中心跳转/客服工单”，再补 Figma。
- ~~上传图片格式/大小/损坏文件基础校验。~~ 已在测试后端和 mock API 完成，支持 jpg/png/webp/heic/heif、默认 9MB 解码文件上限、base64 与图片文件头校验；复用现有上传失败/识别结果页，不需要新增 Figma。后续“裁剪主体选择/多宠确认/人脸隐私提示”等复杂交互仍按第 9 节补设计。
- ~~权限状态持久化、拒绝/永久拒绝后的状态修复。~~ 已补权限页进入刷新、系统设置返回自动刷新、`blocked/unavailable` 不被系统普通 denied 查询降级、发现/地图定位权限结果持久化。
- ~~权限状态接口非法字段/非法状态校验。~~ 已在测试后端和 mock API 完成，复用现有权限页和 toast，不需要新增 Figma。
- ~~设备推送 token 上报字段兜底校验。~~ 已在测试后端和 mock API 完成，token、platform、deviceId 和未知字段非法时返回 `PUSH_DEVICE_INVALID`；复用现有通知授权/通知设置 toast，不需要新增 Figma。
- ~~权限、AI 用量、推送登记和地图定位旧账号回包保护。~~ 已在现有权限页、AI 用量读取、通知登记和地图定位动作内完成账号 token 校验与定位 ref 级互斥；切号或退出后的旧回包不会改当前账号权限、用量、设备 token 或地图定位状态，不需要新增 Figma 页面。
- ~~我的页手机号、宠物信息、当前宠物数据同步。~~ 已接 `GET /me` 和账号快照同步，基础资料会回填到“我的页”；多宠管理和完整资料编辑仍按缺失设计另做。
- ~~当前宠物状态在首页/我的/健康/账号快照之间的一致性。~~ 已补 `activePet` -> `session.account.activePet` 同步，并避免账号快照变化触发重复全量刷新；复用现有页面，不需要新增 Figma。
- ~~当前宠物维度旧回包保护。~~ 已在健康摘要、宠物维度刷新、AI 宠物聊天加载/发送、体重记录、健康备忘和今日小事中校验账号 token 与当前宠物 ID；切号或切宠后的旧回包不会污染当前页面，不需要新增 Figma 页面。
- ~~新用户建档与电子形象保存后的宠物维度数据刷新。~~ 已补建档后健康摘要/体重/疫苗/提醒/备忘/AI 用量刷新，以及保存形象后清理上传/生成草稿；复用现有建档、上传、生成、首页页面，不需要新增 Figma。
- ~~建档/上传/AI 形象生成主链路的防重复与旧任务回写保护。~~ 已在现有宠物信息、上传、识别、生成中、形象确认页内完成 ref 级互斥、账号 token 校验、媒体 ID 校验、任务 ID 校验和路由校验；这是业务状态机修复，不需要新增 Figma 页面。
- ~~宠物建档/编辑字段兜底校验。~~ 已在测试后端和 mock API 完成，复用现有建档/编辑页和 toast，不需要新增 Figma。
- ~~首页/健康页健康摘要数据源切换。~~ 已接 `GET /health/summary`，首页、健康页、体重页和疫苗页顶部摘要会优先使用服务端聚合结果。
- ~~宠物编辑保存接口联动。~~ 已有编辑宠物页会调用 `PATCH /pets/{petId}`，保存后同步当前宠物快照并刷新健康摘要。
- ~~疫苗/驱虫计划状态与提醒字段兜底校验。~~ 已在测试后端和 mock API 完成，疫苗状态只接受 `due/done/overdue`，提醒开关只接受布尔 `enabled`；复用现有疫苗计划页、toast 和通知中心，不需要新增 Figma。
- ~~新增疫苗/驱虫计划。~~ 已在现有疫苗计划页内联实现名称、计划日期、快捷日期、保存 loading 和失败 toast，并接入 `POST /health/vaccines`；不需要新增页面。后续若要做正式犬猫疫苗模板、提醒时间/提前几天等精细规则，再补 Figma 组件状态。
- ~~疫苗/驱虫提醒和标记完成的防重复提交。~~ 已在现有健康页完成 ref 级互斥与退出清理；开启提醒会读取最新通知权限和推送设置再安排本地通知，不需要新增 Figma 页面。
- 健康记录编辑/删除 CRUD 的接口联动；已有新增记录链路，编辑/删除弹层仍需 Figma 设计后再开放。
- ~~发现页下拉刷新、距离计算、附近用户同步。~~ 已支持下拉/按钮刷新、自动定位刷新、3km 模糊距离和基础猫狗/标签筛选；复杂搜索展开、距离范围和 Bottom Sheet 仍需 Figma 设计。
- ~~发现页定位/附近列表旧账号回包保护。~~ 已在现有发现页自动刷新、下拉刷新、定位授权和附近列表加载中完成账号 token 与 request 序号校验；关闭附近可见、切换模糊定位、退出账号或切号后的旧回包不会污染当前页面，不需要新增 Figma 页面。
- ~~我的页附近可见/模糊定位切换后的业务联动。~~ 已复用现有设置行、toast、发现页和消息页完成，不需要新增 Figma；关闭附近可见会清空本机附近列表并暂停发现页自动/手动刷新，重新开启或切换模糊定位会重新定位刷新。
- ~~我的页设置开关并发保存保护。~~ 已在现有设置行完成当前设置 ref 读写和按字段回滚，多个开关并发保存时不会互相覆盖；发现页手动刷新会读取最新附近可见状态，不需要新增 Figma 页面。
- ~~我的页设置开关旧账号回包保护。~~ 已在现有设置行完成账号 token 校验，切号或退出后的旧设置保存回包不会覆盖当前账号设置，不需要新增 Figma 页面。
- ~~消息/招呼请求/未读数的准实时刷新。~~ 已实现轮询、并发保护、当前会话保护和消息页手动刷新；后续富消息/长按菜单仍需 Figma 设计。
- ~~消息页手动刷新状态保护。~~ 已在现有消息页完成 ref 级互斥，快速连点不会让刷新动画提前结束；不需要新增 Figma 页面。
- ~~全量加载、消息刷新和通知已读旧账号回包保护。~~ 已在现有启动数据加载、消息列表刷新、会话记录加载、通知批量已读和会话已读中校验账号 token；切号或退出后的旧回包不会污染当前账号页面，不需要新增 Figma 页面。
- ~~普通聊天发送/重发的防重复与会话归属保护。~~ 已在现有聊天详情页内完成发送互斥、账号 token 校验、当前会话校验，以及本地 sending/failed 气泡按 `conversationId` 过滤；不需要新增 Figma 页面。后续富消息、长按菜单和约遛卡片仍按第 5 节补组件状态。
- ~~打招呼/约遛/招呼请求动作后的现有页面回显。~~ 已补发送中防重复、成功后刷新真实招呼请求/会话/通知中心，复用现有按钮 loading、toast 和消息页，不需要新增 Figma。后续如果要做快捷语、发送成功卡片或约遛富卡片，再按第 6 节补设计。
- ~~打招呼/约遛/招呼请求旧账号回包保护。~~ 已在现有发现页、约遛页和招呼请求页完成账号 token 校验、招呼请求列表 ref 校验、约遛当前对象/路由校验，切号或慢网络不会把旧账号社交结果写入新账号页面；不需要新增 Figma 页面。
- ~~未接受招呼前的普通聊天门禁。~~ 已实现后端硬校验和现有聊天页输入框禁用态；不需要新增页面。后续如果要做更精细的“等待接受”会话卡片样式，再补 Figma 组件状态。
- ~~普通聊天与约遛文本基础安全拦截。~~ 已在服务端和 mock API 完成手机号、邮箱、外链、微信/QQ、明显违法/灰产词拦截；复用现有发送失败 toast，不需要新增页面。后续如果要展示“违规详情/申诉/安全教育卡片”，再补 Figma 组件状态。
- ~~疫苗提醒真机本地通知调度。~~ 已在现有疫苗页/通知设置/退出登录逻辑内完成，不需要新增页面。后续如果要做提醒时间选择、重复规则、提前几天提醒等设置，再补 Figma 设计。
- ~~通知中心健康提醒去重与失效清理。~~ 已在测试后端和 mock API 完成：开启临近/逾期提醒会生成去重通知，关闭提醒或标记完成会清理旧健康提醒；复用现有通知中心，不需要新增页面。
- ~~AI 对话高风险医疗门禁写入健康备忘和就医提醒。~~ 已通过现有 AI 对话页、健康备忘页和通知中心完成，不需要新增页面。后续如果要展示“紧急记录卡片/一键拨打医院/撤销记录”等富状态，再补 Figma 组件状态。
- ~~AI 对话显式写入健康备忘。~~ 已通过现有 AI 对话页和健康备忘接口完成，不需要新增页面。后续如果要展示“已保存备忘卡片/撤销保存”，再补 Figma 组件状态。
- ~~AI 对话显式写入体重记录。~~ 已通过现有 AI 对话页和体重接口完成，不需要新增页面。后续如果要展示“已记录体重卡片/撤销保存”，再补 Figma 组件状态。
- ~~体重记录和健康备忘字段兜底校验。~~ 已在测试后端和 mock API 完成，复用现有体重页、健康备忘页和 toast，不需要新增 Figma。
- ~~AI 对话显式更新疫苗/驱虫计划。~~ 已通过现有 AI 对话页、疫苗计划接口和提醒接口完成，不需要新增页面。后续如果要展示“已更新计划卡片/撤销操作”，再补 Figma 组件状态。
- ~~AI 对话显式更新宠物档案。~~ 已通过现有 AI 对话页、宠物资料接口和账号快照同步完成，不需要新增页面。后续如果要展示“已更新档案卡片/撤销修改/字段确认弹层”，再补 Figma 组件状态。
- ~~AI 对话业务动作后的现有页面回显。~~ 已把 AI 回复携带的档案、体重、备忘、疫苗计划、提醒开关和就医提醒统一同步到首页/健康/我的/通知中心，并复用现有 toast，不需要新增 Figma。后续如果要做“已同步卡片/撤销操作/字段确认弹层”，再补 Figma 组件状态。
- ~~AI 对话长上下文摘要。~~ 已在服务端完成确定性轻量摘要和最近窗口控制，不需要新增页面或 Figma 状态。后续如果要展示“记忆管理/清除记忆/摘要可见化”，再补 Figma 页面。
- ~~AI 对话模型回复后置安全过滤。~~ 已在服务端完成，不需要新增页面或 Figma 状态。后续如果要展示“安全替换说明卡片/为什么建议就医”，再补 Figma 组件状态。
- ~~AI 对话反馈聚合基础能力。~~ 已在服务端完成“像它/不像它”反馈统计和提示词注入，不需要新增页面或 Figma 状态。后续如果要做“反馈历史/个性训练面板”，再补 Figma 页面。
- ~~公开地点内容基础安全拦截。~~ 已在服务端和 mock API 完成，不需要新增页面；现有提交失败 toast 会展示中文错误。后续如果要做“内容违规详情/申诉/人工审核后台”，再补 Figma 页面。
- ~~地点搜索和点评提交旧回包保护。~~ 已在现有地图搜索和地点详情页完成搜索词/当前地点 ref 校验，慢网络旧搜索不会覆盖当前结果，旧点评回包不会清掉新地点草稿；不需要新增 Figma 页面。
- ~~地点收藏、搜索、点评、新增地点旧账号回包保护。~~ 已在现有地图、地点详情和新增地点页完成账号 token 与当前路由校验，切号或退出后的旧回包不会污染收藏列表、地点结果或清空新账号草稿；不需要新增 Figma 页面。
- ~~现有页面的装饰型入口收口。~~ 验证码页语音验证码入口已改为明确 toast；今日小事页心情标签与“AI 帮我写”本地草稿已可联动；新增地点页特色标签会合并进提交内容。均复用现有页面，不需要新增 Figma 页面或状态。
- ~~用户协议/隐私政策占位版静态接口。~~ 已补 `GET /legal/terms`、`GET /legal/privacy`，不需要新增页面；正式 WebView/富文本页面如要做，再由你补 Figma 设计。
- ~~Android 手势返回和退出 App toast 行为。~~ 已补二级页返回、一级 Tab 回首页/空宠物根页、首页二次返回退出 toast、弹窗/地图样式浮层优先关闭；不需要新增设计页。
- 现有页面的布局 Bug、输入框焦点、按钮 loading、toast 自动消失等修复。

## 2026-06-19 新增：宠友圈功能设计需求

来源：`Pet_Friend_Circle_Feature_Spec_2026-06-19.md`。

当前状态：产品草案已补；发布页优先沿用现有「今日小事」页，宠友圈列表、评论弹层和状态合集属于新增复杂页面/组件，进入开发前需要 Figma Make 设计。

### 宠友圈需要补的页面/状态

1. `发现-宠友圈与附近伙伴`：发现页升级为双 Tab，默认宠友圈，另一个 Tab 保留附近伙伴。
2. `宠友圈-动态评论弹层`：评论 Bottom Sheet，含评论列表、空评论、输入栏、发送中/失败状态。
3. `宠友圈-状态合集`：附近暂无小事、附近有伙伴但暂无小事、定位未授权、附近可见关闭、网络失败。
4. `今日小事-可见范围控件`：在现有发布页补「附近宠友可见 / 仅自己可见」选择，不重做整页。

详细提示词已集中写入 `Pet_Friend_Circle_Feature_Spec_2026-06-19.md` 第 14 节，后续不要重复口头散发。

## 已从“缺失页面”清单移除

以下内容历史上曾被当作缺失页面，但现在已有基础实现，因此不再作为 Figma 补图任务发给你：

- ~~登录页/验证码页~~ 已有页面和验证码状态机。
- ~~权限页~~ 已有真实/模拟授权逻辑与下一步流程。
- ~~新用户建档基础链路~~ 已有未添加宠物、宠物信息、上传、识别、生成、确认流程。
- ~~AI 宠物对话基础页~~ 已有基础对话页；只剩富体验增强另行记录。
- ~~发现页基础列表~~ 已有附近用户列表、定位、下拉刷新、距离逻辑。
- ~~地图页基础页~~ 已接高德地图与地点详情基础流程。
- ~~消息列表/通知中心基础页~~ 已有基础页面和数据入口。
- ~~我的页/宠物档案/设置基础页~~ 已有基础页面；只剩多宠管理等增强设计。
