# Lumii MVP 开发记录 2026-06-10

## 本次完成
- 发现页社交闭环补强：
  - 后端新增真实招呼请求列表、接受、婉拒接口。
  - 打招呼时兼容当前已安装 APK，仍会给双方创建会话，方便两台手机不用重装也能互测。
  - 下一版 App 会使用待处理招呼接口，不再把附近用户伪装成招呼请求。
- 消息闭环补强：
  - 会话增加头像、宠物名、ownerId、更新时间等元信息。
  - 打开会话后未读数会同步落后端。
  - 会话头像不再固定使用第一位附近用户头像。
- 后端整理：
  - 删除重复的 `/social/greetings` 和 `/social/walk-invites` 死路由。
  - 同一对用户的待处理招呼不会因为反复点击而刷屏生成消息和通知。

## 已验证
- `npm run typecheck`
- `node --check scripts/lumii-backend.cjs`
- 本地临时后端集成测试：
  - 手机号 A/B 登录。
  - A/B 创建宠物并上报相近定位。
  - A 发现 B。
  - A 向 B 打招呼。
  - B 看到招呼请求并接受。
  - 双方会话建立。
  - B 给 A 发消息，A 出现未读。
  - A 标记会话已读后后端未读数归零。

## 流量策略
- 日常开发不上传 APK 到腾讯云。
- 后端部署只做 `git pull` 和服务重启，流量很小。
- 只有涉及原生配置、地图/相册/通知权限、App 图标、阶段验收时才打 APK。

## 晚间追加
- 健康管理测试后端补强：
  - `GET/POST /health/weights` 改为按当前登录用户和当前宠物持久化。
  - 新增体重记录后同步更新当前宠物 `weightKg`。
  - `GET/POST /health/memos` 改为按当前宠物持久化。
  - `GET /health/vaccines` 改为按当前宠物持久化默认计划。
  - 新增 `PATCH /health/vaccines/{vaccineId}`，支持疫苗计划标记完成。
- 前端数据真实性补强：
  - “我的页”不再在无宠物时展示假的当前宠物。
  - 首页四个信息卡改为读取真实体重、疫苗、备忘、附近伙伴数量。
  - 宠物档案页的生日、体重、性别、毛色、疫苗状态改为真实字段或“待补充/待识别”。
  - 地点详情、今日记录、招呼请求、对话页清理固定“奶油”兜底文案。
- 设置与隐私测试后端补强：
  - 新增 `GET /settings` 和 `PATCH /settings`。
  - 登录账号快照返回用户设置，前端登录恢复时直接使用云端设置。
  - 设置页“模糊定位、附近可见、互动消息提醒、通知”开关改为保存到后端，失败会回滚。
  - 发现页后端继续尊重 `nearbyVisible=false`，关闭后其他用户刷新附近列表不会再看到该账号。

## 晚间验证
- `npm run typecheck`
- `node --check scripts/lumii-backend.cjs`
- 本地临时后端健康链路测试：
  - 手机号登录。
  - 创建宠物。
  - 读取默认体重/备忘/疫苗。
  - 新增体重并再次读取，确认宠物体重同步更新。
  - 新增健康备忘并再次读取。
  - 标记疫苗完成并再次读取。
- 本地临时后端设置链路测试：
  - 手机号 A/B 登录并创建宠物。
  - B 关闭 `nearbyVisible`。
  - A 刷新附近发现，确认 B 不再出现在列表。
  - B 重新读取 `/settings`，确认开关持久化。

## AI 对话策略追加
- DeepSeek V4 接入策略已整理到 `docs/AI_Pet_Chat_DeepSeek_Strategy_2026-06-10.md`。
- MVP 默认策略：
  - `deepseek-v4-flash`
  - `thinking.type=disabled`
  - `max_tokens=420`
  - 最近历史默认只带 10 条消息
  - 单条用户输入默认限制 600 字
- 后端已新增电子宠物聊天历史：
  - `GET /ai/pet-chat/messages`
  - `POST /ai/pet-chat/messages`
- 后端会注入用户不可见的 Lumii 宠物陪伴提示词、当前宠物档案、健康摘要和最近少量历史。
- DeepSeek Key 仅通过服务端环境变量配置，不进入前端、不提交 Git。
- 未配置 DeepSeek Key 时自动使用 fallback 回复，避免开发环境误烧 token。
